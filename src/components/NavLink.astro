---
export interface Props {
    title: string;
    href: string;
}

const { href, title } = Astro.props as Props;
---

<a
    class="text-sm font-semibold mx-2 px-4 py-2 hover:bg-slate-800 rounded-sm text-gray-100 transition duration-200"
    href={href}
    >{title}
</a>
